"""
All API test case definitions.

This list starts empty. Add cases by running:
    python generate_tests.py --api_name NAME --base_url URL --endpoint PATH --demo

Each entry is a dict with keys:
  id, name, description, api_name, base_url, endpoint, method,
  headers, payload, params, expected_status, expected_keys, test_type
"""

TEST_CASES = [
]
